{% extends 'base.html.twig' %}

{% block title %}PABBA - Evènement
{% endblock %}
{% block body %}


	<div class="container">


		<ul class="nav nav-pills nav-fill mb-3 event-index-pills" id="pills-tab" role="tablist">
			<li class="nav-item" style="border:none;" role="presentation">
				<a class="nav-link active" id="today-tab" data-toggle="pill" href="#today" role="tab" aria-controls="today" aria-selected="false">En cours</a>
			</li>
			<li class="nav-item" style="border:none;" role="presentation">
				<a class="nav-link" id="toCome-tab" data-toggle="pill" href="#toCome" role="tab" aria-controls="toCome" aria-selected="true">A venir</a>
			</li>
		</ul>

		<div
			class="tab-content" id="pills-tabContent">

			{# Onglet des évènements en cours #}
			<div class="tab-pane fade show active" id="today" role="tabpanel" aria-labelledby="today-tab">
				<div class="card-columns">
					{% for event in events %}
						{% if isToday(event.eventAt) == 0 %}
							{% include "event/_event_card.html.twig"%}
						{% endif %}
					{% endfor %}
				</div>

			</div>

			{# Onglet des évènements à venir #}
			<div class="tab-pane fade" id="toCome" role="tabpanel" aria-labelledby="toCome-tab">

				<div class="card-columns">
					{% for event in events %}
						{% if isToday(event.eventAt) == 1 %}
							{% include "event/_event_card.html.twig" with { "event" : event } %}
						{% endif %}
					{% endfor %}
				</div>

			</div>

		</div>


	</div>

	<a href="{{ path("event_create") }}" class="btn add-btn bi bi-plus-circle-fill"></a>


{% endblock %}
