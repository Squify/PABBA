{% extends 'base.html.twig' %}

{% block title %}PABBA - Evènement
{% endblock %}

{% block body %}


    <div class="container">
        <div class="row">
            <div class="col-12 mt-5 mt-md-0">
                <!-- Button trigger modal -->
                <button id="open-filter" type="button" data-toggle="modal" data-target="#filterModal" class="btn filter-btn"><i
                        class="bi bi-filter"></i>Filtres
                </button>
                <ul class="nav nav-pills nav-fill mb-3 event-index-pills" id="pills-tab" role="tablist">
                    <li class="nav-item" style="border:none;" role="presentation">
                        <a class="nav-link active" id="today-tab" data-toggle="pill" href="#today" role="tab" aria-controls="today" aria-selected="false">En
                            cours
                        </a>
                    </li>
                    <li class="nav-item" style="border:none;" role="presentation">
                        <a class="nav-link" id="toCome-tab" data-toggle="pill" href="#toCome" role="tab" aria-controls="toCome" aria-selected="true">A venir</a>
                    </li>
                </ul>

                <div class="tab-content" id="events-list">

                </div>


            </div>
        </div>

    </div>

    <a href="{{ path("event_create") }}" class="btn add-btn bi bi-plus-circle-fill"></a>

    <div class="modal right fade" id="filterModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog ">
            <div class="modal-content" id="filter-content">

            </div>
        </div>
    </div>


{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('event_search') }}
    {{ encore_entry_script_tags('event_create') }}
{% endblock %}
